<Page
    x:Class="ATLAS.Pages.LinkAnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    Background="Transparent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="20" Padding="40">
            <TextBlock Text="Link Analysis" FontSize="32" FontWeight="SemiBold"/>
            <TextBlock Text="Enter a suspicious URL or link below to check it against a database of known malicious sites." TextWrapping="Wrap"/>

            <TextBox x:Name="UrlTextBox" Header="URL to Analyze" KeyDown="UrlTextBox_KeyDown" PlaceholderText="https://example.com"/>

            <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,10,0,0">
                <Button x:Name="AnalyzeButton"
                        Content="Run Analysis"
                        Style="{ThemeResource AccentButtonStyle}"
                        Click="AnalyzeButton_Click"/>
                <ProgressRing x:Name="LoadingRing" IsActive="False"/>
            </StackPanel>

            <Grid x:Name="ResultsBox" Visibility="Collapsed" Margin="0,24,0,0" Padding="24" 
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="1" 
              CornerRadius="8"
              HorizontalAlignment="Stretch">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="8" VerticalAlignment="Center" Margin="0,0,24,0">
                    <TextBlock Text="Analysis result" Style="{ThemeResource BodyStrongTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon x:Name="StatusIcon" Glyph="&#xE73E;" FontSize="24" Foreground="SeaGreen" VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" Text="Safe" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock x:Name="ExplanationText" Text="No security vendors and no sandboxes flagged this as malicious." 
                   Style="{ThemeResource BodyTextBlockStyle}" 
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="24" VerticalAlignment="Center">

                    <StackPanel Spacing="4" MinWidth="64">
                        <TextBlock x:Name="MaliciousText" Text="0" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" HorizontalAlignment="Center"/>
                        <TextBlock Text="Malicious" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Spacing="4" MinWidth="64">
                        <TextBlock x:Name="SuspiciousText" Text="0" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" HorizontalAlignment="Center"/>
                        <TextBlock Text="Suspicious" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Spacing="4" MinWidth="64">
                        <TextBlock x:Name="HarmlessText" Text="0" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" HorizontalAlignment="Center"/>
                        <TextBlock Text="Harmless" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Spacing="4" MinWidth="64">
                        <TextBlock x:Name="UndetectedText" Text="94" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" HorizontalAlignment="Center"/>
                        <TextBlock Text="Undetected" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>