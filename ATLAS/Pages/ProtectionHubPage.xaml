<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ATLAS.Pages.ProtectionHubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    Background="Transparent">

    <SplitView x:Name="MySplitView"
               IsPaneOpen="True"
               PanePlacement="Left"
               DisplayMode="Inline"
               OpenPaneLength="240"
               PaneBackground="Transparent">

        <SplitView.Pane>
            <ListView x:Name="ProtectionSelectionList"
                      SelectionMode="Single"
                      SelectionChanged="ProtectionSelectionList_SelectionChanged"
                      Background="Transparent">
                <ListViewItem Tag="FileScan">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE7C3;"/>
                        <TextBlock Text="File Scan"/>
                    </StackPanel>
                </ListViewItem>
            </ListView>
        </SplitView.Pane>

        <SplitView.Content>
            <Frame x:Name="ProtectionContentFrame"/>
        </SplitView.Content>

    </SplitView>
</Page>