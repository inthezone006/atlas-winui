<Page
    x:Class="ATLAS.Pages.TextAnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="40">
        <StackPanel Spacing="20" MaxWidth="800">
            <TextBlock Text="Text Analysis" FontSize="32" FontWeight="SemiBold"/>
            <TextBlock Text="Paste the suspicious text message, email, or social media post below to analyze it for potential signs of a scam." TextWrapping="Wrap"/>

            <TextBox x:Name="InputTextBox"
                     Header="Content to Analyze"
                     PlaceholderText="Paste content here..."
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="200" />

            <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,10,0,0">
                <Button x:Name="AnalyzeButton"
                        Content="Run Analysis"
                        Style="{ThemeResource AccentButtonStyle}"
                        Click="AnalyzeButton_Click"/>
                <ProgressRing x:Name="LoadingRing" IsActive="False"/>
            </StackPanel>

            <Border x:Name="ResultsBox"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="20"
                    Margin="0,20,0,0"
                    Visibility="Collapsed">
                <StackPanel Spacing="15">
                    <TextBlock Text="Analysis Results" FontSize="20" FontWeight="SemiBold"/>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="Interpretation:" FontWeight="SemiBold"/>
                        <TextBlock x:Name="InterpretationText" Text="Scam Likely" Foreground="OrangeRed"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="Confidence Score:" FontWeight="SemiBold"/>
                        <TextBlock x:Name="ScoreText" Text="95%"/>
                    </StackPanel>

                    <StackPanel Spacing="5">
                        <TextBlock Text="Explanation:" FontWeight="SemiBold"/>
                        <TextBlock x:Name="ExplanationText" Text="The message contains urgent language designed to create panic..." TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>