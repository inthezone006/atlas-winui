<Page
    x:Class="ATLAS.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    Background="Transparent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ATLAS.Models">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="50">
                <HyperlinkButton x:Name="BackButton"
                             Click="BackButton_Click"
                             HorizontalAlignment="Left"
                             Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <FontIcon Glyph="&#xE72B;" FontSize="12"/>
                        <TextBlock Text="Back to Dashboard"/>
                    </StackPanel>
                </HyperlinkButton>
                <TextBlock x:Name="PageTitle" Text="History" FontSize="32" FontWeight="SemiBold" Margin="0,0,0,20"/>
                <ProgressRing x:Name="LoadingRing" IsActive="True" HorizontalAlignment="Center"/>
                <ListView x:Name="HistoryListView" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:AnalysisHistoryItem">
                            <Expander Margin="0,5" HorizontalAlignment="Stretch" Expanding="Expander_Expanding" HorizontalContentAlignment="Left">
                                <Expander.Header>
                                    <StackPanel Margin="0,5">
                                        <TextBlock Text="{x:Bind AnalysisType, Converter={StaticResource TitleCaseConverter}}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind CreatedAt}" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                </Expander.Header>
                                <Expander.Content>
                                    <ProgressRing IsActive="True" Margin="20"/>
                                </Expander.Content>
                            </Expander>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>