<Page
    x:Class="ATLAS.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ATLAS.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="10" Padding="40">
            <TextBlock x:Name="WelcomeTextBlock"
                       FontSize="32"
                       FontWeight="SemiBold"
                       Margin="0,0,0,20"/>

            <GridView SelectionMode="None" HorizontalAlignment="Center">

                <Button Tag="total_analyses"
                        Click="StatCard_Click"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Padding="20" MinWidth="320" Height="280" Margin="10">
                        <StackPanel Spacing="15">
                            <TextBlock Text="Your Statistics" FontSize="18" FontWeight="SemiBold"/>
                            <StackPanel>
                                <TextBlock x:Name="TotalAnalysesText" Text="-" FontSize="28"/>
                                <TextBlock Text="Analyses Ran" Opacity="0.8"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="ScamsDetectedText" Text="-" FontSize="28"/>
                                <TextBlock Text="Scams Detected" Opacity="0.8"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="SubmissionsText" Text="-" FontSize="28"/>
                                <TextBlock Text="Submissions Approved" Opacity="0.8"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Button>

                <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Padding="20" MinWidth="320" Height="280" Margin="10">
                    <StackPanel Spacing="15" VerticalAlignment="Center">
                        <TextBlock Text="Submit New Scam" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Help improve ATLAS by submitting new scam examples you might encounter." TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>
                        <Button x:Name="SubmitScamButton" Content="Submit a New Scam" HorizontalAlignment="Center" Click="SubmitScamButton_Click" Style="{ThemeResource AccentButtonStyle}"/>
                    </StackPanel>
                </Border>
            </GridView>
        </StackPanel>
    </ScrollViewer>
</Page>